# תוכנית שיפור וניקוי קוד - יהב הצלה בטוחה
מסמך זה מרכז את כלל המשימות הנדרשות לשיפור הארכיטקטורה, האבטחה וביצועי המערכת (Code Review & Refactoring), על בסיס הסריקה שבוצעה. 
כל סעיף כולל הסבר בעברית. נסמן ב-[x] משימות שהושלמו.

## 1. אבטחה ותיקוני באגים קריטיים (Security & Bugs)
- [x] **תיקון חור אבטחה בגישה לקבצים (IDOR ב-`/uploads`):** נוספה חסימה בקוד שמוודאת שעובד יכול לגשת אך ורק לתלושי השכר שלו עצמו.
- [x] **ביטול שינויי מסד נתונים ישירים (Manual Migrations):** הקוד הוסר מ-`main.py` ובוצעה בדיקת תקינות. בעתיד תוגדר מערכת Alembic לניהול הגרסאות.

## 2. ארכיטקטורה וביצועים (Architecture & Performance)
- [ ] **פירוק ראוטרים שמנים (Fat Routers):** קבצים כמו `reports.py` מכילים בתוכם לוגיקה עסקית ושאילתות מורכבות למסד הנתונים (כמו יצירת ספקים במקום הלא נכון). יש להפריד את השאילתות לשכבת `Repository` או `Service` כדי שהקוד יהיה קריא, ניתן לבדיקה ומודולרי.
- [ ] **הזרקת תלויות נכונה (Dependency Injection):** כרגע `PayrollService` נוצר ידנית בתוך הפונקציה (לדוגמה כשמנהל מאשר דוח). יש להעביר אותו להזרקה נכונה דרך `Depends()` של FastAPI.
- [ ] **שיפור ביצועי שליפת נתונים (N+1 Queries):** בפונקציה של `matrix` (מטריצת דוחות למנהל), המערכת קוראת נתונים בצורה פחות יעילה (שולפת שיבוצים ואז שולפת דוחות בנפרד ומחברת בעזרת מילון פייתון). אפשר לייעל את זה עם Join טבעי למסד הנתונים כדי לחסוך זמן עיבוד ועומס שרת.

## 3. ניקיון קוד (Clean Code)
- [ ] **החלפת טקסטים "קשיחים" (Magic Strings):** הקוד משתמש במחרוזות טקסט ישירות כמו `"שכיר"` או `"approved"`. אם מישהו טועה באות אחת, המערכת תיכשל בשקט ללא שגיאת קומפילציה. נחליף את כולם ל-Enums מסודרים (למשל `EmploymentType.EMPLOYEE`).
- [ ] **איחוד קוד כפול (DRY):** הלוגיקה של הגשת דוח על ידי מנהל זהה כמעט לחלוטין ללוגיקה של הגשת דוח על ידי העובד בראוטר של הדוחות. יש לאחד את הבדיקות כדי שלא נצטרך לתחזק כפילויות.
